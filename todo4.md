---
title: "Фаза 4: Коллаборация и экспорт"
created: 2025-12-06
modified: 2025-12-06
category: project
status: active
tags: [api, apollo-next, frontend]
related:
  - TZ
  - TZ-v2.0
  - ukraine-poi-data-sources-report
  - todo2
  - todo3
---
# Фаза 4: Коллаборация и экспорт
**Приоритет:** 🟡 Средний
**Статус:** ⏸️ Ожидает завершения Фазы 3
**Зависимости:** Фаза 1 + Фаза 2 + Фаза 3

---

## 4.1 Управление доступами

### Приглашение пользователей
- [ ] Кнопка "Поделиться картой" в header
- [ ] Модальное окно с формой приглашения
- [ ] Поле ввода email
- [ ] Выбор уровня доступа (Editor / Viewer)
- [ ] Отправка email-приглашения через Supabase

### Список участников
- [ ] Отображение всех участников карты
- [ ] Аватар + email + роль
- [ ] Владелец выделен особо
- [ ] Иконка онлайн-статуса (опционально)

### Управление ролями
- [ ] Изменение роли участника (Owner может)
- [ ] Удаление участника из карты
- [ ] Подтверждение опасных действий

### Передача владения
- [ ] Owner может передать карту другому пользователю
- [ ] Подтверждение через email
- [ ] Логирование передачи

---

## 4.2 Режим презентации

### Создание презентации
- [ ] Кнопка "Создать презентацию" в toolbar
- [ ] Режим выбора точек (чекбоксы на маркерах)
- [ ] Панель выбранных точек (3-10 штук)
- [ ] Превью презентации

### Настройки презентации
- [ ] Название презентации
- [ ] Описание (опционально)
- [ ] Срок действия ссылки (7 дней по умолчанию)
- [ ] Пароль (опционально)
- [ ] Скрыть комментарии (да/нет)

### Генерация ссылки
- [ ] Уникальный короткий URL (UUID или hash)
- [ ] Копирование в буфер обмена
- [ ] QR-код для ссылки (опционально)
- [ ] Отправка по email

### Просмотр презентации (для получателя)
- [ ] Минимальный интерфейс (без sidebar)
- [ ] Только выбранные точки на карте
- [ ] Информация о точках в popup
- [ ] Переключение между точками (стрелки)
- [ ] Zoom to fit all points
- [ ] Водяной знак "APOLLO NEXT" (опционально)

### Управление презентациями
- [ ] Список созданных презентаций
- [ ] Статистика просмотров
- [ ] Деактивация ссылки
- [ ] Удаление презентации

---

## 4.3 Экспорт в Excel

### Формат экспорта
Таблица с колонками:
- [ ] Город
- [ ] Группа/Бренд
- [ ] Название точки
- [ ] Статус
- [ ] Адрес
- [ ] Координаты (lat, lng)
- [ ] Ссылка на Google Maps
- [ ] Последний комментарий
- [ ] Дата создания
- [ ] Дата обновления

### Фильтрация перед экспортом
- [ ] Модальное окно "Экспорт"
- [ ] Выбор городов (мультиселект)
- [ ] Выбор групп (мультиселект)
- [ ] Выбор статусов
- [ ] Диапазон дат
- [ ] Превью количества записей

### Форматы экспорта
- [ ] Excel (.xlsx) — основной
- [ ] CSV — дополнительный
- [ ] JSON — для разработчиков (опционально)
- [ ] KML/KMZ — для Google Earth (опционально)

### Генерация файла
- [ ] Подключить библиотеку xlsx
- [ ] Форматирование колонок (ширина, выравнивание)
- [ ] Автофильтры в Excel
- [ ] Заморозка первой строки (headers)
- [ ] Цветовое кодирование статусов

### UI экспорта
- [ ] Кнопка "Экспорт" в toolbar
- [ ] Индикатор прогресса для больших наборов
- [ ] Автоматическое скачивание файла
- [ ] Уведомление об успешном экспорте

---

## 4.4 Импорт данных

### Импорт из Excel
- [ ] Загрузка .xlsx / .csv файла
- [ ] Маппинг колонок (drag & drop)
- [ ] Валидация данных
- [ ] Превью перед импортом
- [ ] Обработка ошибок (пропуск строк с ошибками)

### Импорт из KMZ/KML
- [ ] Уже реализовано в v1.4.2
- [ ] Улучшить парсинг стилей
- [ ] Автоопределение групп из папок

### Массовое обновление
- [ ] Обновление существующих точек по названию/координатам
- [ ] Добавление только новых точек
- [ ] Выбор режима (merge / replace)

---

## 4.5 Дополнительные функции коллаборации

### Уведомления
- [ ] Email-уведомления о новых точках
- [ ] Уведомления о комментариях
- [ ] Дайджест изменений (ежедневный/еженедельный)
- [ ] Настройки уведомлений в профиле

### Активность
- [ ] Лог активности на карте
- [ ] Кто добавил/изменил точку
- [ ] Фильтр по пользователю
- [ ] Фильтр по дате

### Real-time обновления (опционально)
- [ ] Supabase Realtime подписки
- [ ] Мгновенное отображение изменений других пользователей
- [ ] Индикатор "X пользователей онлайн"
- [ ] Курсоры других пользователей на карте

---

## Критерии завершения Фазы 4

- [ ] Можно пригласить пользователей по email с разными ролями
- [ ] Можно создать публичную ссылку на презентацию (3-10 точек)
- [ ] Презентация открывается без авторизации
- [ ] Можно экспортировать данные в Excel с фильтрацией
- [ ] Работает импорт из Excel/CSV

---

## Технические зависимости

```json
{
  "xlsx": "^0.18.x",
  "file-saver": "^2.x",
  "qrcode.react": "^3.x",
  "uuid": "^9.x"
}
```

## База данных (дополнительные таблицы)

```sql
-- Презентации
CREATE TABLE presentations (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  map_id UUID REFERENCES maps(id),
  user_id UUID REFERENCES users(id),
  name TEXT NOT NULL,
  description TEXT,
  point_ids UUID[] NOT NULL,
  share_token TEXT UNIQUE NOT NULL,
  password_hash TEXT,
  expires_at TIMESTAMP WITH TIME ZONE,
  hide_comments BOOLEAN DEFAULT false,
  view_count INTEGER DEFAULT 0,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Приглашения
CREATE TABLE invitations (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  map_id UUID REFERENCES maps(id),
  inviter_id UUID REFERENCES users(id),
  email TEXT NOT NULL,
  access_level TEXT NOT NULL,
  token TEXT UNIQUE NOT NULL,
  accepted_at TIMESTAMP WITH TIME ZONE,
  expires_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Лог активности
CREATE TABLE activity_log (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  map_id UUID REFERENCES maps(id),
  user_id UUID REFERENCES users(id),
  action TEXT NOT NULL,
  entity_type TEXT NOT NULL,
  entity_id UUID,
  details JSONB,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## Компоненты для создания

```
src/components/
├── Sharing/
│   ├── ShareModal.jsx
│   ├── InviteForm.jsx
│   ├── MemberList.jsx
│   └── RoleSelector.jsx
├── Presentation/
│   ├── CreatePresentationModal.jsx
│   ├── PointSelector.jsx
│   ├── PresentationPreview.jsx
│   └── PresentationView.jsx (public page)
├── Export/
│   ├── ExportModal.jsx
│   ├── ExportFilters.jsx
│   └── ExportProgress.jsx
├── Import/
│   ├── ImportModal.jsx
│   ├── ColumnMapper.jsx
│   └── ImportPreview.jsx
└── Activity/
    ├── ActivityFeed.jsx
    └── ActivityItem.jsx
```

## API endpoints

```
# Презентации
POST   /presentations              - создать презентацию
GET    /presentations              - список презентаций
GET    /presentations/:token       - получить публичную презентацию
DELETE /presentations/:id          - удалить презентацию

# Приглашения
POST   /invitations                - отправить приглашение
GET    /invitations/accept/:token  - принять приглашение
DELETE /map_access/:userId         - удалить участника

# Экспорт
POST   /export                     - экспорт данных (возвращает файл)

# Активность
GET    /maps/:mapId/activity       - лог активности
```

## Публичные страницы

```
/p/:token           - просмотр презентации (без авторизации)
/invite/:token      - принятие приглашения
```
